apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "calendar-app.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "calendar-app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "calendar-app.name" . }}
    spec:
      containers:
        - name: calendar
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8080
